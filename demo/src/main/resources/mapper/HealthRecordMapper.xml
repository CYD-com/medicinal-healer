<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usergenerator.mapper.HealthRecordMapper">
    <select id="selectByUserId" resultType="com.example.usergenerator.entity.HealthRecord">
        SELECT id, user_id, height, weight, blood_type, marital_status,
               health_score, total_visits, total_prescriptions, last_visit_date,
               created_at, updated_at
        FROM health_record
        WHERE user_id = #{userId}
        LIMIT 1
    </select>
</mapper>
