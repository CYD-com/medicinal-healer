<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usergenerator.mapper.AppointmentMapper">
    <resultMap id="BaseResultMap" type="com.example.usergenerator.entity.Appointment">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="department_id" property="departmentId"/>
        <result column="appointment_date" property="appointmentDate"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="status" property="status"/>
        <result column="patient_name" property="patientName"/>
        <result column="patient_phone" property="patientPhone"/>
        <result column="symptoms" property="symptoms"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <sql id="Base_Column_List">id, user_id, doctor_id, department_id, appointment_date, start_time, end_time, status, patient_name, patient_phone, symptoms, create_time, update_time</sql>
    <select id="selectAppointmentsByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_appointment WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    <select id="selectAppointmentsByDoctorId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_appointment WHERE doctor_id = #{doctorId} ORDER BY appointment_date ASC, start_time ASC
    </select>
    <select id="selectAppointmentsByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_appointment WHERE status = #{status} ORDER BY create_time DESC
    </select>
    <select id="selectAppointmentsByDate" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_appointment WHERE appointment_date = #{appointmentDate} ORDER BY start_time ASC
    </select>
    <select id="selectAppointmentWithDetails" resultMap="BaseResultMap">
        SELECT a.* FROM t_appointment a LEFT JOIN t_doctor d ON a.doctor_id = d.id LEFT JOIN t_department dept ON a.department_id = dept.id WHERE a.id = #{id}
    </select>
    <select id="countAppointmentsByDoctorAndDate" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_appointment WHERE doctor_id = #{doctorId} AND appointment_date = #{appointmentDate}
    </select>
    <select id="countAppointmentsByDoctorDateAndTime" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_appointment WHERE doctor_id = #{doctorId} AND appointment_date = #{appointmentDate} AND (start_time &lt; #{endTime} AND end_time &gt; #{startTime})
    </select>
</mapper>