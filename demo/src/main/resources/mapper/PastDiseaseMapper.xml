<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usergenerator.mapper.PastDiseaseMapper">
    <select id="selectByUserId" resultType="com.example.usergenerator.entity.PastDisease">
        SELECT id, user_id, disease_name, diagnosis_date, diagnosis_hospital,
               current_status, treatment, remark, created_at, updated_at
        FROM past_disease
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>
</mapper>
